spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eticketsdb
    username: jimkata
    password: p@SSw0r2
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: never